# hello-dock — starter

This is the starting point for the hello-dock project. It contains a working React + Vite + TypeScript app with no Docker support. You will add Docker support as you follow the handbook chapters.
